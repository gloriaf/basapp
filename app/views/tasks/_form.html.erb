<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :code %><%= f.text_field :code %></p>
  <p><%= f.label :title %><%= f.text_field :title %></p>
  <p><%= f.label :sequence %><%= f.number_field :sequence %></p>
  <p><%= f.label :rroute %><%= f.text_field :rroute %></p>
  <p><%= f.label :description %><%= f.text_field :description %></p>
  <p><%= f.label :general %><%= f.check_box :general %></p>
  <p><%= f.label :active %><%= f.check_box :active %></p>
  <p><%= f.submit "Update" %></p>

<% end %>
