<% provide(:title, 'Task') %>

  <div class="normal">
    <div class="formulario">
      <%= render 'form' %>
    </div></br></br>
  </div>
  
  <div class="normal">
    <p>
      <%= link_to t('tasks.show_link'), @task %> |
      <%= link_to t('tasks.back'), tasks_path %>
    </p>
  </div>
  
