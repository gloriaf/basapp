<div class="normal">
  <p><%= t('.hello') %> <%= @resource.email %>!</p>
  
  <p><%= t('.your_account') %></p>
  
  <p><%= t('.click') %></p>
  
  <p><%= link_to t('.unlock'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
</div>